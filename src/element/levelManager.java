package element;
import java.awt.image.*;
public class levelManager {
    BufferedImage [] levelSprite ;
    public void importOutsideSprite(){
        BufferedImage img = LoadSave.background("Terrain (16x16)");
        levelSprite = new BufferedImage[33];
        for (int i = 0 ; i<)
    }
}
